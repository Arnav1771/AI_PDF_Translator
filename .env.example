# Copy this file to .env and fill in your key if you use a build/test script.
# The extension itself does NOT read this file at runtime.
# Set your API key directly in the extension popup (chrome://extensions → TranslateExt → popup).
GEMINI_API_KEY=your_gemini_api_key_here
